import jwt from "jsonwebtoken";


const generateAccessToken = (response, userId) => {
    
};


const generateRefreshToken = (response, userId) => {

};

const deleteTokens = (response, userId) => {

};


export {
    generateAccessToken,
    generateRefreshToken,
    deleteTokens
};